Comandos:
1) Crear archivo .env (ya creado de ejemplo) y ajustar contraseñas si es necesario.
2) docker compose build
3) docker compose up
App: http://localhost:3000
Oracle: localhost:1521/XEPDB1 (SYSTEM/${ORACLE_PASSWORD}) y usuario APP_USER/app_password
