<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Editar estadía</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <header class="container">
    <h1>Editar estadía</h1>
    <nav class="nav">
      <a href="/">Inicio</a>
      <% if (user) { %>
        <a href="/estadias">Volver</a>
        <span style="flex:1"></span>
        <span>Hola, <strong><%= user.username %></strong></span>
        <a href="/logout">Salir</a>
      <% } else { %>
        <a href="/login">Login</a>
        <a href="/register">Registro</a>
      <% } %>
    </nav>
  </header>
  <main class="container">
    <form class="card form" method="post" action="/estadias/<%= item.ID_RESERVA %>/<%= item.COD_HOTEL %>/<%= item.FECHA_LLEGADA %>">
      <label>ID Reserva</label>
      <input value="<%= item.ID_RESERVA %>" disabled />
      <label>Código Hotel</label>
      <input value="<%= item.COD_HOTEL %>" disabled />
      <label>Fecha llegada</label>
      <input value="<%= item.FECHA_LLEGADA %>" disabled />
      <label>Fecha partida</label>
      <input name="partida" value="<%= item.FECHA_PARTIDA %>" required />
      <label>Régimen</label>
      <input name="regimen" value="<%= item.REGIMEN %>" required />
      <button type="submit">Actualizar</button>
    </form>
  </main>
</body>
</html>
