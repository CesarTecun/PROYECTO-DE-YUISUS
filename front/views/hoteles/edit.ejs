<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Editar hotel</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <header class="container">
    <h1>Editar hotel</h1>
    <nav class="nav">
      <a href="/">Inicio</a>
      <% if (user) { %>
        <a href="/sucursales">Sucursales</a>
        <a href="/hoteles">Hoteles</a>
        <a href="/vuelos">Vuelos</a>
        <a href="/turistas">Turistas</a>
        <a href="/reservas">Reservas</a>
        <span style="flex:1"></span>
        <span>Hola, <strong><%= user.username %></strong></span>
        <a href="/logout">Salir</a>
      <% } else { %>
        <a href="/login">Login</a>
        <a href="/register">Registro</a>
      <% } %>
    </nav>
  </header>
  <main class="container">
    <form class="card form" method="post" action="/hoteles/<%= item.COD_HOTEL %>">
      <label>Código (no editable)</label>
      <input value="<%= item.COD_HOTEL %>" disabled />
      <label>Nombre</label>
      <input name="nombre" value="<%= item.NOMBRE %>" required />
      <label>Dirección</label>
      <input name="direccion" value="<%= item.DIRECCION %>" />
      <label>Ciudad</label>
      <input name="ciudad" value="<%= item.CIUDAD %>" />
      <label>Teléfono</label>
      <input name="telefono" value="<%= item.TELEFONO %>" />
      <label>Plazas totales</label>
      <input type="number" name="plazas" min="0" value="<%= item.PLAZAS_TOTALES %>" />
      <button type="submit">Actualizar</button>
    </form>
  </main>
</body>
</html>
