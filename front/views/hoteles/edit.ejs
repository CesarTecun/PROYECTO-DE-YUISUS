<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Editar hotel</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <div class="layout">
    <aside class="sidebar">
      <%- include('../partials/sidebar', { user: user, active: 'hoteles' }) %>
    </aside>
    <section class="main">
      <%- include('../partials/topbar', { user: user }) %>
      <div class="content">
        <div class="panel" style="max-width:560px">
          <h1>Editar hotel</h1>
          <form class="form" method="post" action="/hoteles/<%= item.COD_HOTEL %>">
            <label>Código (no editable)</label>
            <input class="input" value="<%= item.COD_HOTEL %>" disabled />
            <label>Nombre</label>
            <input class="input" name="nombre" value="<%= item.NOMBRE %>" required />
            <label>Dirección</label>
            <input class="input" name="direccion" value="<%= item.DIRECCION %>" />
            <label>Ciudad</label>
            <input class="input" name="ciudad" value="<%= item.CIUDAD %>" />
            <label>Teléfono</label>
            <input class="input" name="telefono" value="<%= item.TELEFONO %>" />
            <label>Plazas totales</label>
            <input class="input" type="number" name="plazas" min="0" value="<%= item.PLAZAS_TOTALES %>" />
            <div style="display:flex;gap:10px;justify-content:flex-end;margin-top:8px">
              <a class="btn" href="/hoteles" style="border-color:#334155;color:#e5e7eb">Cancelar</a>
              <button class="btn btn-success" type="submit">Actualizar</button>
            </div>
          </form>
        </div>
      </div>
    </section>
  </div>
</body>
</html>
