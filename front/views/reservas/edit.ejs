<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <title>Editar Reserva - Agencia de Viajes</title>
  <link rel="stylesheet" href="/styles.css" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    .input-with-icon {
      position: relative;
      margin-bottom: 1.5rem;
    }
    .input-with-icon i {
      position: absolute;
      left: 12px;
      top: 42px;
      color: #6b7280;
    }
    .input-with-icon input, 
    .input-with-icon select {
      padding-left: 40px !important;
      width: 100%;
    }
    .form-actions {
      display: flex;
      gap: 1rem;
      justify-content: flex-end;
      margin-top: 2rem;
      padding-top: 1.5rem;
      border-top: 1px solid #e5e7eb;
    }
    .btn-auth i {
      margin-right: 8px;
    }
    .form-header {
      margin-bottom: 2rem;
      text-align: center;
    }
    .form-header i {
      font-size: 2.5rem;
      color: #3b82f6;
      margin-bottom: 1rem;
    }
    .form-title {
      font-size: 1.5rem;
      font-weight: 600;
      color: #111827;
      margin-bottom: 0.5rem;
    }
    .form-subtitle {
      color: #6b7280;
      margin-bottom: 1.5rem;
    }
    .card {
      margin-bottom: 2rem;
    }
    .table {
      width: 100%;
      border-collapse: collapse;
      margin: 1rem 0;
    }
    .table th, .table td {
      padding: 0.75rem;
      text-align: left;
      border-bottom: 1px solid #e5e7eb;
    }
    .table th {
      background-color: #f9fafb;
      font-weight: 600;
      color: #374151;
    }
  </style>
</head>
<body>
  <div class="layout">
    <aside class="sidebar">
      <%- include('../partials/sidebar', { user: user, active: 'reservas' }) %>
    </aside>
    <section class="main">
      <%- include('../partials/topbar', { user: user }) %>
      <div class="content">
        <form class="auth-card" method="post" action="/reservas/<%= item.ID_RESERVA %>" style="max-width: 800px; margin: 0 auto;">
          <div class="form-header">
            <i class="fas fa-calendar-check"></i>
            <h1 class="form-title">Editar Reserva #<%= item.ID_RESERVA %></h1>
            <p class="form-subtitle">Actualice los datos de la reserva</p>
          </div>
          
          <% if (error) { %>
            <div class="alert error" style="margin-bottom: 1.5rem;">
              <i class="fas fa-exclamation-circle"></i> <%= error %>
            </div>
          <% } %>
          
          <div class="input-with-icon">
            <i class="fas fa-ticket-alt"></i>
            <label for="idReserva">ID Reserva</label>
            <input id="idReserva" type="text" class="form-control" 
                   value="<%= item.ID_RESERVA %>" disabled />
          </div>
          
          <div class="input-with-icon">
            <i class="far fa-calendar"></i>
            <label for="fechaReserva">Fecha de Reserva</label>
            <input id="fechaReserva" type="text" class="form-control" 
                   value="<%= item.FECHA_RESERVA %>" disabled />
          </div>
          
          <div class="input-with-icon">
            <i class="fas fa-user-tag"></i>
            <label for="codTurista">Código de Turista</label>
            <input id="codTurista" name="codTurista" type="text" class="form-control" 
                   value="<%= item.COD_TURISTA %>" required 
                   placeholder="Ingrese el código del turista" />
          </div>
          
          <div class="input-with-icon">
            <i class="fas fa-store"></i>
            <label for="codSucursal">Código de Sucursal</label>
            <input id="codSucursal" name="codSucursal" type="text" class="form-control" 
                   value="<%= item.COD_SUCURSAL %>" required 
                   placeholder="Ingrese el código de la sucursal" />
          </div>
          
          <div class="form-actions">
            <a href="/reservas" class="btn-auth" style="background-color: #6b7280;">
              <i class="fas fa-times"></i> Cancelar
            </a>
            <button type="submit" class="btn-auth">
              <i class="fas fa-save"></i> Actualizar Reserva
            </button>
          </div>
        </form>
      </div>
    </section>
  </div>
</body>
</html>
