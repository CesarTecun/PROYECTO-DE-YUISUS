<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Sucursales</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <div class="layout">
    <aside class="sidebar">
      <%- include('../partials/sidebar', { user: user, active: 'sucursales' }) %>
    </aside>
    <section class="main">
      <%- include('../partials/topbar', { user: user }) %>
      <div class="content">
        <div class="table-card">
          <div class="table-toolbar">
            <span style="flex:1"></span>
            <a class="btn btn-success" href="/sucursales/new">Crear sucursal</a>
          </div>
          <table class="table">
            <thead>
              <tr>
                <th>Código</th>
                <th>Dirección</th>
                <th>Teléfono</th>
                <th class="th-right">Acciones</th>
              </tr>
            </thead>
            <tbody>
              <% for (const it of items) { %>
                <tr>
                  <td><%= it.COD_SUCURSAL %></td>
                  <td><%= it.DIRECCION %></td>
                  <td><%= it.TELEFONO %></td>
                  <td>
                    <div class="td-actions" style="justify-content:flex-end">
                      <a class="btn-edit" href="/sucursales/<%= it.COD_SUCURSAL %>/edit">Editar</a>
                      <form method="post" action="/sucursales/<%= it.COD_SUCURSAL %>/delete" onsubmit="return confirm('¿Eliminar sucursal <%= it.COD_SUCURSAL %>?');">
                        <button class="btn-delete" type="submit">Eliminar</button>
                      </form>
                    </div>
                  </td>
                </tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>
    </section>
  </div>
</body>
</html>
