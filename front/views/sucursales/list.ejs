<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Sucursales</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <header class="container">
    <h1>Sucursales</h1>
    <nav class="nav">
      <a href="/">Inicio</a>
      <% if (user) { %>
        <a href="/sucursales/new">Nueva sucursal</a>
        <span style="flex:1"></span>
        <span>Hola, <strong><%= user.username %></strong></span>
        <a href="/logout">Salir</a>
      <% } else { %>
        <a href="/login">Login</a>
        <a href="/register">Registro</a>
      <% } %>
    </nav>
  </header>
  <main class="container">
    <div class="card">
      <table class="table">
        <thead>
          <tr><th>Código</th><th>Dirección</th><th>Teléfono</th></tr>
        </thead>
        <tbody>
          <% for (const it of items) { %>
            <tr>
              <td><%= it.COD_SUCURSAL %></td>
              <td><%= it.DIRECCION %></td>
              <td><%= it.TELEFONO %></td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </main>
</body>
</html>
