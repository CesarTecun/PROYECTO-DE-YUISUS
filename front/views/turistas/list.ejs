<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Turistas</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <header class="container">
    <h1>Turistas</h1>
    <nav class="nav">
      <a href="/">Inicio</a>
      <% if (user) { %>
        <a href="/turistas/new">Nuevo turista</a>
        <span style="flex:1"></span>
        <span>Hola, <strong><%= user.username %></strong></span>
        <a href="/logout">Salir</a>
      <% } else { %>
        <a href="/login">Login</a>
        <a href="/register">Registro</a>
      <% } %>
    </nav>
  </header>
  <main class="container">
    <div class="card">
      <table class="table">
        <thead>
          <tr>
            <th>Código</th><th>Nombre</th><th>Apellidos</th><th>País</th><th>Acciones</th>
          </tr>
        </thead>
        <tbody>
          <% for (const it of items) { %>
            <tr>
              <td><%= it.COD_TURISTA %></td>
              <td><%= [it.NOMBRE1, it.NOMBRE2, it.NOMBRE3].filter(Boolean).join(' ') %></td>
              <td><%= [it.APELLIDO1, it.APELLIDO2].filter(Boolean).join(' ') %></td>
              <td><%= it.PAIS_RESIDENCIA || '' %></td>
              <td>
                <a href="/turistas/<%= it.COD_TURISTA %>/edit">Editar</a>
                <form method="post" action="/turistas/<%= it.COD_TURISTA %>/delete" style="display:inline" onsubmit="return confirm('¿Eliminar turista <%= it.COD_TURISTA %>?');">
                  <button type="submit">Eliminar</button>
                </form>
              </td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </main>
</body>
</html>
