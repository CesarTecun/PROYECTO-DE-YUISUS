<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Editar vuelo</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <div class="layout">
    <aside class="sidebar">
      <%- include('../partials/sidebar', { user: user, active: 'vuelos' }) %>
    </aside>
    <section class="main">
      <%- include('../partials/topbar', { user: user }) %>
      <div class="content">
        <div class="panel" style="max-width:560px">
          <h1>Editar vuelo</h1>
          <form class="form" method="post" action="/vuelos/<%= item.NUM_VUELO %>">
            <label>Número de vuelo (no editable)</label>
            <input class="input" value="<%= item.NUM_VUELO %>" disabled />
            <label>Fecha y hora (YYYY-MM-DDTHH:MM)</label>
            <input class="input" name="fechaHora" value="<%= item.FECHA_HORA %>" required />
            <label>Origen</label>
            <input class="input" name="origen" value="<%= item.ORIGEN %>" required />
            <label>Destino</label>
            <input class="input" name="destino" value="<%= item.DESTINO %>" required />
            <label>Plazas totales</label>
            <input class="input" type="number" name="plazasTotales" min="0" value="<%= item.PLAZAS_TOTALES %>" />
            <label>Plazas turista</label>
            <input class="input" type="number" name="plazasTurista" min="0" value="<%= item.PLAZAS_TURISTA %>" />
            <div style="display:flex;gap:10px;justify-content:flex-end;margin-top:8px">
              <a class="btn" href="/vuelos" style="border-color:#334155;color:#e5e7eb">Cancelar</a>
              <button class="btn btn-success" type="submit">Actualizar</button>
            </div>
          </form>
        </div>
      </div>
    </section>
  </div>
</body>
</html>
